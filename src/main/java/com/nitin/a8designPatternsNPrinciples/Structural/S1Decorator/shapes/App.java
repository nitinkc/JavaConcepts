package com.nitin.a8designPatternsNPrinciples.Structural.S1Decorator.shapes;

/**
 * Requirement is to add the functionality later and
 */
public class App {
    /*public static void main(String[] args) {
        Shape circle = new Circle(5);
        ShapeDecorator shapeDecorator = new TransparentShapeDecorator(circle);
       */ //new Shape(new ShadedShapeDecorator(new Circle(5.5)).draw());
}
